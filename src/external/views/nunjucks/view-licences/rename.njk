{% extends "./nunjucks/layout.njk" %}
{% from "summary-list/macro.njk" import govukSummaryList %}
{% from "forms/index.njk" import formRender, formErrorSummary %}
{% from "nunjucks/view-licences/macros/licence-summary.njk" import licenceSummary %}

{% block content %}

  {{ formErrorSummary(form) }}

  {% if summary.documentName.length %}
    {{ title('Rename licence number ' + summary.licenceNumber) }}
  {% else %}
    {{ title('Name licence number ' + summary.licenceNumber) }}
  {% endif %}

    {{ formRender(form) }}

{% endblock %}
