{
  "id": "/wr22/2.6",
  "type": "object",
  "title": "2.6",
  "description": "Abstraction shall not exceed [max abstraction rate] when the flow in the [water body name] as gauged by the Agency at its flow gauging station at [gauging station] at [gauging station NGR] is equal to or less than [HoF rate] for [HoF duration] as may be notified by the Agency. The Agency’s said gauging of the flow shall be conclusive.",
  "category": "Hands off flows/levels",
  "subcategory": "CAMS",
  "properties": {

    "max_rate": {
      "type": "number",
      "label": "Maximum rate",
      "errors": {
        "required": {
          "type": "Maximum rate should be a number",
          "message": "Select a maximum rate"
        }
      }
    },
    "max_rate_unit": {
      "type" : "string",
      "enum" : ["m³/s"],
      "errors": {
        "required": {
          "message": "Select a rate unit"
        }
      }
    },

    "water_body_name": {
      "$ref": "water://picklists/water_bodies.json",
      "errors": {
        "required": {
          "message": "Select the water body"
        }
      }
    },
    "gauging_station" : {
      "$ref": "water://picklists/gauging_stations.json"
    },
    "gauging_station_ngr": {
      "$ref": "water://types/ngr.json",
      "label": "Gauging station NGR",
      "errors": {
        "pattern": {
          "message": "Measurement point NGR should be a valid grid reference"
        }
      }
    },

    "hof_rate": {
      "type": "number",
      "label": "HoF rate",
      "errors": {
        "required": {
          "type": "HoF rate should be a number",
          "message": "Select a maximum rate"
        }
      }
    },
    "hof_rate_unit": {
      "type" : "string",
      "enum" : ["m³/s"],
      "errors": {
        "required": {
          "message": "Select a HoF rate unit"
        }
      }
    },
    "hof_duration" : {
      "type" : "string"
    }

  },
  "required": ["max_rate", "max_rate_unit", "water_body_name", "gauging_station", "gauging_station_ngr", "hof_rate", "hof_rate_unit", "hof_duration"]
}
