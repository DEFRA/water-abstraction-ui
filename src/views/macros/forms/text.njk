{% from "input/macro.njk" import govukInput %}

{% macro formTextWidget(field) %}

  {% if field.options.type == 'hidden' %}
    <input type="hidden" name="{{ field.name }}" value="{{ field.value }}" />
  {% else %}

  {% set options = field | mapFormField %}

  {{ govukInput(options) }}

  {% endif %}
{% endmacro %}
